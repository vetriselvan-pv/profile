{
 "version": 2,
 "builds": [
  {
   "src": "server/src/server.ts",
   "use": "@vercel/node",
   "config": { "includeFiles": ["server/dist/**"] }
  }
 ],
 "routes": [
  {
   "src": "server/(.*)",
   "dest": "server/dist/server.js"
  }
 ]
}